{
  "name": "wasmtools-binaryxml",
  "version": "1.0.0",
  "author": "Maurice Lam",
  "scripts": {
    "build": "npm run compile-wasm && node build.mjs",
    "watch": "BUILD_MODE=dev node build.mjs",
    "compile-wasm": "cd abxml-wrapper && wasm-pack build --target web --out-dir ../pkg --release && cd .."
  },
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "esbuild-plugin-wasm": "^1.1.0",
    "esbuild-plugin-wasm-pack": "^1.1.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
