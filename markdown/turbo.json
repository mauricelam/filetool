{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "../dist/markdown/**"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "!.turbo/**",
        "../components/**",
        "../esbuild-plugins/**"
      ]
    }
  }
}
