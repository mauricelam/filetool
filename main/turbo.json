{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "../dist/index.html",
        "../dist/libmagic-wrapper.wasm",
        "../dist/main.js",
        "../dist/main.js.map",
        "../dist/icons/**"
      ],
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "!.turbo/**"
      ]
    }
  }
}
