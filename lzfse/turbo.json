{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "../dist/lzfse/**"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "!.turbo/**",
        "../components/**",
        "../esbuild-plugins/**"
      ]
    }
  }
}
