{
  "name": "protoscope",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "BUILD_MODE=dev node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  }
}
