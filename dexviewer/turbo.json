{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "../dist/dexviewer/**"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "!.turbo/**",
        "../components/**",
        "../esbuild-plugins/**"
      ]
    }
  }
}
