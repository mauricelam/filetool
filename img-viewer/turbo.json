{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "../dist/img-viewer/**"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "!.turbo/**",
        "../components/**",
        "../esbuild-plugins/**"
      ]
    }
  }
}
