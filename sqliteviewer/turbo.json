{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "../dist/sqliteviewer/**"
      ],
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "!.turbo/**"
      ]
    }
  }
}
