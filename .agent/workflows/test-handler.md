---
description: Test handler
---

When testing a handler, you need to create a corresponding file type (e.g. an mp4 file for the ffmpeg handler), and then paste it or upload it into the browser via the main page. (Directly opening the URL inside the iframe is not going to work)